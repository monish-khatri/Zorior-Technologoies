# Zorior Technologies Practical

## Steps to Setup:
- Import `Database/users.sql`
- Change Database configuration in `model/DB.php::__construct()`
- use `admin` as user name or password
- update profile
